version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: turfvision_postgres
    restart: always
    environment:
      POSTGRES_USER: turfvision
      POSTGRES_PASSWORD: turfvision_pass
      POSTGRES_DB: turfvision_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U turfvision -d turfvision_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: turfvision_redis
    restart: always
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
